"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
export default function Signup() { const [message, setMessage] = useState(""); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const values = Object.fromEntries(new FormData(e.currentTarget)); const response = await fetch("/api/signup", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(values) }); if (response.ok) window.location.assign("/login"); else setMessage((await response.json()).error ?? "We couldn’t create your workspace."); } return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="card w-full max-w-md"><div className="grid h-10 w-10 place-items-center rounded-xl bg-indigo-600 font-black text-white">L</div><h1 className="mt-5 text-2xl font-bold">Create your workspace</h1><p className="mt-1 text-sm text-[var(--muted)]">Bring every customer signal into one place.</p><div className="mt-6 space-y-4"><label className="form-label">Your name<input name="name" required minLength={2} /></label><label className="form-label">Workspace name<input name="workspace" required minLength={2} placeholder="Acme Inc." /></label><label className="form-label">Work email<input name="email" type="email" required /></label><label className="form-label">Password<input name="password" type="password" minLength={12} required placeholder="At least 12 characters" /></label></div>{message && <p className="mt-4 text-sm text-rose-600">{message}</p>}<button className="primary-button mt-6 w-full">Create workspace</button><p className="mt-5 text-center text-sm text-[var(--muted)]">Already have access? <Link href="/login" className="font-semibold text-indigo-600">Sign in</Link></p></form></main>; }
